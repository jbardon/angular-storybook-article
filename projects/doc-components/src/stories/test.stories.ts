import { Meta, StoryObj } from '@storybook/angular';
import { LibComponentsComponent } from 'lib-components';

const meta: Meta<LibComponentsComponent> = {
  title: 'ngx-pumpkin/Form/Input',
  component: LibComponentsComponent,
};

export default meta;

export const Default: StoryObj<LibComponentsComponent> = {};

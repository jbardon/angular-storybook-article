import { Meta, Canvas } from '@storybook/blocks';
import * as stories from './example-8.stories';
import fileSource from './file-code.txt?raw';

<Meta of={stories}/>

<Canvas of={stories.NoCode} sourceState="none"/>

export const inlineSource = `MyToasterService.toast('Inline code', 'Title');`;

<Canvas of={stories.InlineCode} source={{code: inlineSource}}/>

<Canvas of={stories.FileCode} source={{code: fileSource}}/>